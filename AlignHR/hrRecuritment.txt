We are building an HR Recruitment module in ASP.NET Core 8 MVC with Entity Framework Core and SQL Server.

Existing modules already exist:

Users
Employees
Departments
Role-Based Access Control (RBAC)

Primary keys use:

NUMERIC(18,0) IDENTITY(1,1)

for all tables.

We will implement the Recruitment module incrementally.

For Sprint 1, create ONLY the Requisition module.

Business requirements:

A Line Manager can create a requisition request.
Requisition can be saved as Draft.
Requisition can be Submitted.
No approval workflow yet.
No candidate management yet.
No job posting yet.
No ATS yet.
Every requisition belongs to a department.
Every requisition is created by an employee.
Requisition numbers must be unique.

Status values:

Draft
Submitted
Cancelled

Create:

SQL table
Entity Framework model
Fluent configuration
Repository interface
Repository implementation
Service interface
Service implementation
MVC Controller
ViewModel

Use clean architecture patterns already used in the project.

Primary key type must be:

decimal

to match:

NUMERIC(18,0)

Audit fields required:

CreatedBy
CreatedOn
UpdatedBy
UpdatedOn


No workflow implementation yet.

First Table Only

I would start with exactly one table:

CREATE TABLE HR_Requisition
(
    Id NUMERIC(18,0) IDENTITY(1,1) NOT NULL PRIMARY KEY,

    RequisitionNo NVARCHAR(30)  NULL,

    EmployeeFK NUMERIC(18,0) NULL,

    DepartmentFK NUMERIC(18,0) NULL,

    PositionTitle NVARCHAR(200)  NULL,

    VacancyCount  NUMERIC(18,0) NULL,

    Reason NVARCHAR(1000) NULL,

    Status NVARCHAR(30)  NULL,

    RequiredByDate DATE NULL,

    CreatedBy Nvarchar(50)  NULL,

    CreatedOn DATETIME2  NULL,

    UpdatedBy Nvarchar(50) NULL,

    UpdatedOn DATETIME2 NULL,
);
Why only this table?

Because the workflow always starts here:

Resignation
    ↓
Requisition Created
    ↓
Requisition Submitted
    ↓
Approval
    ↓
Recruitment

Without a requisition, nothing else exists.

So the safest first milestone is:

Deliverable 1
Create Requisition
Edit Requisition
View Requisition
Submit Requisition
List Requisitions

Sprint 2 Goal

Business flow:

Line Manager
    ↓ Submit
HRBP
    ↓ Approve
HOD
    ↓ Approve
Recruiter Assigned

We want a flexible workflow, not hardcoded approval columns.

We already have HR_Requisition.

Now implement a generic approval workflow for requisitions.

Requirements:

Workflow must support multiple approval levels.
Approval levels must be configurable from database.
Workflow must not be hardcoded.
Requisition moves through workflow steps.
Every approval action must be audited.
Support:
Approve
Reject
Send Back
Store complete approval history.
Use NUMERIC(18,0) IDENTITY primary keys.
Use decimal type in Entity Framework.

Current workflow:

LM Submit
↓
HRBP Review
↓
HOD Approval
↓
Recruiter Assignment

Design should allow adding future levels without code changes.

Create:

Tables
EF Entities
Fluent Configurations
Services
Repository Layer
MVC Screens

Use clean architecture.

Table 1: Workflow Definition

This table defines the process.

CREATE TABLE HR_WorkflowDefinition
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    WorkflowName NVARCHAR(100) NOT NULL,

    IsActive BIT NOT NULL DEFAULT(1),

   CreatedBy Nvarchar(50)  NULL,

    CreatedOn DATETIME2  NULL,

    UpdatedBy Nvarchar(50) NULL,

    UpdatedOn DATETIME2 NULL,
);

Example data:

Recruitment Requisition Approval
Table 2: Workflow Steps

Defines approval sequence.

CREATE TABLE HR_WorkflowStep
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    WorkflowDefinitionId NUMERIC(18,0) NOT NULL,

    StepOrder INT NOT NULL,

    StepName NVARCHAR(100) NOT NULL,

    RoleId NUMERIC(18,0) NOT NULL,

    IsFinalStep BIT NOT NULL DEFAULT(0),

    CreatedBy Nvarchar(50)  NULL,

    CreatedOn DATETIME2  NULL,

    UpdatedBy Nvarchar(50) NULL,

    UpdatedOn DATETIME2 NULL,
);

Example:

1 HRBP
2 HOD
3 Recruiter Assignment
Table 3: Workflow Instance

When a requisition enters workflow.

CREATE TABLE HR_WorkflowInstance
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    WorkflowDefinitionId NUMERIC(18,0) NOT NULL,

    EntityType NVARCHAR(50) NOT NULL,

    EntityId NUMERIC(18,0) NOT NULL,

    CurrentStepId NUMERIC(18,0) NOT NULL,

    Status NVARCHAR(50) NOT NULL,

    StartedDate DATETIME2 NOT NULL,

    CompletedDate DATETIME2 NULL
);

Example:

EntityType = Requisition
EntityId = 145
CurrentStep = HRBP
Status = Pending
Table 4: Approval History

Most important table.

CREATE TABLE HR_WorkflowHistory
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    WorkflowInstanceFK NUMERIC(18,0) NOT NULL,

    WorkflowStepFK NUMERIC(18,0) NOT NULL,

    ActionBy NUMERIC(18,0) NOT NULL,

    ActionType NVARCHAR(50) NOT NULL,

    Comments NVARCHAR(1000) NULL,

    ActionDate DATETIME2 NOT NULL
);

ActionType:

Approved
Rejected
SentBack
Submitted
Small Change to Requisition Table

Add:

ALTER TABLE HR_Requisition
ADD WorkflowInstanceFK NUMERIC(18,0) NULL;
Expected Deliverable

After Sprint 2:

A manager creates a requisition.

Draft
 ↓
Submit
 ↓
Workflow Instance Created
 ↓
HRBP Queue
 ↓
Approve
 ↓
HOD Queue
 ↓
Approve
 ↓
Completed

Every action is stored in HR_WorkflowHistory.
Sprint 3 Goal

Once a requisition is fully approved:

Assign a recruiter
Create one or more job postings
Publish internally
Publish externally

Still NO candidates yet.
The next logical step from the diagram is:

Approved Requisition
        ↓
Recruiter Assignment
        ↓
Job Posting

We already have:

HR_Requisition
Workflow Engine
Approval History

Now implement Recruiter Assignment and Job Posting.

Requirements:

Only approved requisitions can be assigned.
One requisition has one assigned recruiter.
One requisition can have multiple job postings.
Support Internal Posting.
Support External Posting.
Job posting status must be tracked.
Audit fields required.
Soft delete required.
Use NUMERIC(18,0) IDENTITY primary keys.
Use decimal in EF Core entities.

Create:

Tables
Entities
Fluent Configurations
Repository Layer
Service Layer
MVC Pages

No candidate functionality yet.

Table 1: Recruiter Assignment
CREATE TABLE HR_RequisitionAssignment
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    RequisitionFk NUMERIC(18,0)  NULL,

    RecruiterEmployeeFK NUMERIC(18,0)  NULL,

    AssignedDate DATETIME2  NULL,

    Notes NVARCHAR(1000) NULL,

    CreatedBy Nvarchar(50)  NULL,

    CreatedOn DATETIME2  NULL,

    UpdatedBy Nvarchar(50) NULL,

    UpdatedOn DATETIME2 NULL,
);
Table 2: Job Posting

One requisition may create multiple postings.

Example:

Software Engineer
 ├── Internal Career Portal
 ├── LinkedIn
 ├── Indeed
CREATE TABLE HR_JobPosting
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    RequisitionFK NUMERIC(18,0)  NULL,

    JobCode NVARCHAR(30)  NULL,

    JobTitle NVARCHAR(200)  NULL,

    JobDescription NVARCHAR(1000) NULL,

    EmploymentType NVARCHAR(50) NULL,

    Location NVARCHAR(200) NULL,

    PostingStatus NVARCHAR(30) NOT NULL,

    OpenDate DATE NOT NULL,

    CloseDate DATE NULL,

    CreatedBy Nvarchar(50)  NULL,

    CreatedOn DATETIME2  NULL,

    UpdatedBy Nvarchar(50) NULL,

    UpdatedOn DATETIME2 NULL,
);
Table 3: Posting Channels

Avoid hardcoding LinkedIn, Internal Portal, etc.

CREATE TABLE HR_PostingChannel
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    ChannelName NVARCHAR(100) NOT NULL,

    IsInternal BIT NOT NULL,

    IsActive BIT NOT NULL DEFAULT(1)
);

Seed:

Internal Portal
LinkedIn
Indeed
Naukri
Company Website
Table 4: Job Posting Channel

Many-to-many relationship.

CREATE TABLE HR_JobPostingChannel
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    JobPostingFK NUMERIC(18,0) NOT NULL,

    PostingChannelFK NUMERIC(18,0) NOT NULL,

    PublishedDate DATETIME2 NULL,

    ExternalReference NVARCHAR(500) NULL
);
Suggested Statuses
Requisition
Draft
Submitted
Approved
Rejected
Cancelled
Job Posting
Draft
Published
Paused
Closed
Deliverable After Sprint 3

Business flow becomes:

Manager Creates Requisition
            ↓
Workflow Approval
            ↓
Approved
            ↓
Recruiter Assigned
            ↓
Job Posting Created
            ↓
Published

Recruiters can now manage openings properly.
Important Architecture Advice

Before Sprint 4, ask Codex to create a generic master table for statuses instead of storing magic strings like:

Approved
Rejected
Published
Closed

If you wait until Candidates and Interviews are built, changing statuses becomes painful.

So after Sprint 3, I'd actually do a small refactor:

Status Master
Status Category
Status Values

Sprint 4 Goal

Business flow:

Job Posted
      ↓
Candidate Applies
      ↓
Recruiter Reviews
      ↓
Candidate Moves Through Stages
      ↓
Shortlist

Still NO offers and NO onboarding yet.
We already have:

Requisition
Workflow
Recruiter Assignment
Job Posting

Now implement ATS (Applicant Tracking System).

Requirements:

Candidates can apply to a job posting.
Resume upload required.
Candidate profile must be reusable across multiple applications.
One candidate can apply to multiple jobs.
Application stages must be tracked.
Full stage history required.
No interview scheduling yet.
No offer management yet.
Audit fields required.

Primary key = NUMERIC(18,0) IDENTITY.
EF Core type = decimal.

Create:

Tables
Entities
Repositories
Services
MVC Screens
Table 1: Candidate

Never store candidate data directly in applications.

A candidate may apply multiple times.

CREATE TABLE HR_Candidate
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    FirstName NVARCHAR(100) NOT NULL,

    LastName NVARCHAR(100) NULL,

    Email NVARCHAR(200) NOT NULL,

    Phone NVARCHAR(50) NULL,

    CurrentLocation NVARCHAR(200) NULL,

    TotalExperienceYears DECIMAL(5,2) NULL,

    CurrentEmployer NVARCHAR(200) NULL,

    CurrentDesignation NVARCHAR(200) NULL,

    LinkedInProfile NVARCHAR(500) NULL,

    CreatedDate DATETIME2  NULL,

    ModifiedDate DATETIME2 NULL,

    IsDeleted BIT NOT NULL DEFAULT(0)
);
Table 2: Candidate Document

A candidate can upload multiple files.

CREATE TABLE HR_CandidateDocument
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    CandidateFk NUMERIC(18,0) N NULL,

    DocumentType NVARCHAR(50)  NULL,

    FileName NVARCHAR(500)  NULL,

    FilePath NVARCHAR(1000)  NULL,

    UploadedDate DATETIME2  NULL
);

Examples:

Resume
Cover Letter
Certification
Passport
Table 3: Job Application

This connects:

Candidate
      ↓
Job Posting
CREATE TABLE HR_JobApplication
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    CandidateFK NUMERIC(18,0)  NULL,

    JobPostingFK NUMERIC(18,0)  NULL,

    CurrentStageFK NUMERIC(18,0)  NULL,

    AppliedDate DATETIME2  NULL,

    RecruiterNotes NVARCHAR(MAX) NULL,

    IsActive BIT NOT NULL DEFAULT(1)
);
Table 4: Application Stage Master

Don't hardcode stages.

CREATE TABLE HR_ApplicationStage
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    StageName NVARCHAR(100) NOT NULL,

    StageOrder INT  NULL,

    IsActive BIT NOT NULL DEFAULT(1)
);

Seed data:

Applied
Screening
Shortlisted
Interview
Final Review
Selected
Rejected
Withdrawn
Table 5: Application Stage History

This is the most important ATS table.

CREATE TABLE HR_ApplicationStageHistory
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    JobApplicationFk NUMERIC(18,0)  NULL,

    FromStageId NUMERIC(18,0) NULL,

    ToStageFk NUMERIC(18,0)  NULL,

    ChangedBy NUMERIC(18,0)  NULL,

    Comments NVARCHAR(1000) NULL,

    ChangedDate DATETIME2  NULL
);
Required ATS Screens
Recruiter Dashboard
Open Jobs
Applications Count
Candidates in Pipeline
Candidate List
Candidate Name
Email
Experience
Current Stage
Applied Date
Application Pipeline
Applied
 ↓
Screening
 ↓
Shortlisted
 ↓
Interview
 ↓
Selected

Recruiter can drag or move candidates between stages.

Deliverable After Sprint 4

The business flow becomes:

Approved Requisition
        ↓
Job Posting
        ↓
Candidate Applies
        ↓
Application Created
        ↓
Stage Tracking
        ↓
Shortlisted
Before Sprint 5

I would ask Claude to implement one extra feature:

File Storage Service

Create an abstraction:

IFileStorageService
Local Storage
through configuration.

Do NOT save resumes inside SQL Server.

Store only paths/URLs in HR_CandidateDocument.

---------------------------------------------------
              Sprint -5 
---------------------------------------------------

Now comes Sprint 5: Interviews.

At this stage you have:

Requisition
 ↓
Approval Workflow
 ↓
Recruiter Assignment
 ↓
Job Posting
 ↓
Candidate
 ↓
Application Pipeline

The next business process is:

Shortlisted Candidate
        ↓
Interview Scheduling
        ↓
Interview Feedback
        ↓
Selection Decision
Important Design Change Before Coding

Most HR systems make a mistake:

Interview 1
Interview 2
Interview 3

stored as columns.

Don't do that.

Different jobs need different rounds.

Example:

Developer
-----------
HR Screening
Technical Round
System Design
Manager Round

Accountant
-----------
HR Screening
Finance Round

CEO
-----------
Board Round 1
Board Round 2

Therefore interviews must be configurable.
We already have:

Requisition
Workflow
Recruiter Assignment
Job Posting
Candidate
Job Application
Application Stage History

Now implement Interview Management.

Requirements:

A job posting can define interview rounds.
Different jobs may have different numbers of rounds.
Multiple interviewers can participate in one round.
Interview feedback must be stored.
Candidate scorecards required.
Interview history required.
Recruiter schedules interviews.
Candidate can move through interview rounds.
Full audit trail required.
Primary key NUMERIC(18,0) IDENTITY.
EF Core type decimal.

Create:

Tables
Entities
Services
MVC Screens

No offer management yet.

Table 1: Interview Round Master

Defines interview structure.

CREATE TABLE HR_InterviewRound
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    JobPostingId NUMERIC(18,0) NOT NULL,

    RoundName NVARCHAR(100) NOT NULL,

    RoundOrder INT NOT NULL,

    IsMandatory BIT NOT NULL DEFAULT(1),

    IsActive BIT NOT NULL DEFAULT(1)
);

Examples:

HR Screening
Technical Round
System Design
Manager Round
Table 2: Interview Schedule

Actual interview appointment.

CREATE TABLE HR_InterviewSchedule
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    JobApplicationId NUMERIC(18,0) NOT NULL,

    InterviewRoundId NUMERIC(18,0) NOT NULL,

    ScheduledDateTime DATETIME2 NOT NULL,

    MeetingLink NVARCHAR(1000) NULL,

    Location NVARCHAR(500) NULL,

    Status NVARCHAR(50) NOT NULL,

    CreatedBy NUMERIC(18,0) NOT NULL,

    CreatedDate DATETIME2 NOT NULL
);

Status:

Scheduled
Completed
Cancelled
Rescheduled
NoShow
Table 3: Interview Panel

One interview may have many interviewers.

CREATE TABLE HR_InterviewPanel
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    InterviewScheduleId NUMERIC(18,0) NOT NULL,

    EmployeeFk NUMERIC(18,0) NOT NULL
);

Example:

System Design Round

Interviewer A
Interviewer B
Interviewer C
Table 4: Interview Feedback

Most important table.

CREATE TABLE HR_InterviewFeedback
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    InterviewScheduleFk NUMERIC(18,0) NOT NULL,

    InterviewerEmployeeFk NUMERIC(18,0) NOT NULL,

    OverallScore DECIMAL(5,2) NULL,

    Recommendation NVARCHAR(50) NOT NULL,

    Strengths NVARCHAR(MAX) NULL,

    Concerns NVARCHAR(MAX) NULL,

    Comments NVARCHAR(MAX) NULL,

    SubmittedDate DATETIME2 NOT NULL
);

Recommendation:

Strong Hire
Hire
Hold
Reject
Table 5: Evaluation Criteria

Allows scorecards.

CREATE TABLE HR_EvaluationCriteria
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    JobPostingId NUMERIC(18,0) NOT NULL,

    CriteriaName NVARCHAR(200) NOT NULL,

    MaxScore DECIMAL(5,2) NOT NULL
);

Examples:

Communication
Problem Solving
Leadership
Technical Skills
System Design
Table 6: Criteria Score
CREATE TABLE HR_EvaluationScore
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    InterviewFeedbackFk NUMERIC(18,0) NOT NULL,

    EvaluationCriteriaFk NUMERIC(18,0) NOT NULL,

    Score DECIMAL(5,2) NOT NULL
);
Deliverable After Sprint 5

Business process becomes:

Candidate Applied
        ↓
Screening
        ↓
Interview Scheduled
        ↓
Feedback Submitted
        ↓
Next Round
        ↓
Final Decision

Recruiters can now track every interview and interviewer evaluation.

One Thing I'd Tell Claude

Do not store:

Result
Pass/Fail

only.

Always store:

Score
Recommendation
Comments
Criteria Scores

because later management will ask:

Why was candidate rejected?
Average technical score?
Best interviewer?
Time between rounds?
Hiring quality metrics?

Those become impossible if you only store Pass/Fail.

-----------------------------------------------------------------
                        Sprint 6 
-----------------------------------------------------------------

Now you're at Sprint 6: Offer Management & Hiring Decision.

At this point the system can:

Requisition
 ↓
Approval
 ↓
Job Posting
 ↓
Applications
 ↓
Interviews
 ↓
Selection

The next business event is:

Selected Candidate
        ↓
Offer Approval
        ↓
Offer Letter
        ↓
Candidate Response
        ↓
Hire

This is where many ATS systems separate into a Recruiting module and a Pre-Onboarding module.
We already have:

Requisition
Workflow Engine
Job Posting
Candidate Management
Interview Management

Now implement Offer Management.

Requirements:

Only selected candidates can receive offers.
Multiple offer revisions must be supported.
Offer approval workflow required.
Offer acceptance and rejection required.
Candidate response tracking required.
Offer documents must be stored.
Audit trail required.
Primary key NUMERIC(18,0) IDENTITY.
EF Core decimal type.

Create:

Tables
Entities
Services
MVC Screens

Do not implement onboarding yet.

Table 1: Offer

Core offer record.

CREATE TABLE HR_Offer
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    JobApplicationFk NUMERIC(18,0)  NULL,

    OfferNumber NVARCHAR(50)  NULL,

    ProposedSalary DECIMAL(18,2) NOT NULL,

    ProposedJoiningDate DATE NULL,

    ExpiryDate DATE NULL,

    Status NVARCHAR(50)  NULL,

    CreatedBy Nvarchar(50)  NULL,

    CreatedOn DATETIME2  NULL,

    UpdatedBy Nvarchar(50) NULL,

    UpdatedOn DATETIME2 NULL,
);
Table 2: Offer Version

Never overwrite salary changes.

Track revisions.

CREATE TABLE HR_OfferVersion
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    OfferFk NUMERIC(18,0)  NULL,

    VersionNo INT  NULL,

    Salary DECIMAL(18,2)  NULL,

    JoiningDate DATE NULL,

    Remarks NVARCHAR(1000) NULL,

    CreatedBy NUMERIC(18,0)  NULL,

    CreatedDate DATETIME2  NULL
);

Example:

Version 1 = 80,000
Version 2 = 90,000
Version 3 = 95,000
Table 3: Offer Approval

Separate from requisition approval.

CREATE TABLE HR_OfferApproval
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    OfferFk NUMERIC(18,0)  NULL,

    ApproverEmployeeFk NUMERIC(18,0)  NULL,

    ApprovalLevel INT  NULL,

    Status NVARCHAR(50)  NULL,

    Comments NVARCHAR(1000) NULL,

    ActionDate DATETIME2 NULL
);
Table 4: Offer Document

Generated PDFs.

CREATE TABLE HR_OfferDocument
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    OfferFk NUMERIC(18,0)  NULL,

    FileName NVARCHAR(500)  NULL,

    FilePath NVARCHAR(1000) NOT NULL,

    GeneratedDate DATETIME2  NULL
);

Store file path only.

Do not store PDF binary in SQL Server.

Table 5: Candidate Offer Response

Critical table.

CREATE TABLE HR_OfferResponse
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    OfferFk NUMERIC(18,0)  NULL,

    ResponseType NVARCHAR(50) NOT NULL,

    ResponseDate DATETIME2 NOT NULL,

    Comments NVARCHAR(1000) NULL
);

ResponseType:

Accepted
Rejected
NegotiationRequested
Expired
Recommended Offer Statuses
Draft
PendingApproval
Approved
Sent
Accepted
Rejected
Expired
Cancelled
Important Business Rule

Do NOT close the requisition when the offer is created.

Do NOT close the requisition when the offer is sent.

Close only when:

Offer Accepted
      ↓
Candidate Marked Hired

Otherwise a candidate rejecting an offer will force recruiters to reopen everything.

Additional Table (Very Useful)

Track final hiring decision.

CREATE TABLE HR_HiringDecision
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    JobApplicationFk NUMERIC(18,0)  NULL,

    Decision NVARCHAR(50) NOT NULL,

    DecisionBy NUMERIC(18,0)  NULL,

    DecisionDate DATETIME2  NULL,

    Remarks NVARCHAR(1000) NULL
);

Decision:

Selected
Rejected
Hold
Hired
Deliverable After Sprint 6

Workflow becomes:

Application
      ↓
Interviews
      ↓
Selected
      ↓
Offer Approval
      ↓
Offer Sent
      ↓
Candidate Accepts
      ↓
Hired

At this point, you have a complete ATS.
----------------------------------------------------------
		 Sprint 7 
----------------------------------------------------------
Sprint 7: Onboarding & Employee Conversion

At this point you have:

Requisition
 ↓
Approval
 ↓
Job Posting
 ↓
Candidates
 ↓
Interviews
 ↓
Offer
 ↓
Accepted
 ↓
Hired

Now we move to:

Hired Candidate
       ↓
Pre-Onboarding
       ↓
IT Tasks
       ↓
Manager Tasks
       ↓
Document Collection
       ↓
Employee Created
Important Architecture Decision

Since you already have an Employee module:

Do NOT create employee records immediately after offer acceptance.

Create employee records only after:

Candidate Status = Joined

This prevents:

Ghost employees
No-shows
Offer accepted but never joined
We already have:

Requisition
Workflow
Job Posting
ATS
Interviews
Offers

Now implement Onboarding.

Requirements:

Onboarding starts when candidate is marked Hired.
Employee record is NOT created yet.
Onboarding checklist required.
Different departments can receive onboarding tasks.
Document collection required.
Employee record created only when candidate joins.
Audit trail required.
NUMERIC(18,0) IDENTITY primary keys.
EF Core decimal type.

Create:

Tables
Entities
Services
MVC Screens

Integrate with existing Employee module.

Table 1: Onboarding

Main onboarding record.

CREATE TABLE HR_Onboarding
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    CandidateId NUMERIC(18,0) NOT NULL,

    OfferId NUMERIC(18,0) NOT NULL,

    PlannedJoiningDate DATE NOT NULL,

    ActualJoiningDate DATE NULL,

    Status NVARCHAR(50) NOT NULL,

    CreatedBy Nvarchar(50)  NULL,

    CreatedOn DATETIME2  NULL,

    UpdatedBy Nvarchar(50) NULL,

    UpdatedOn DATETIME2 NULL,

);
Table 2: Onboarding Task Template

Reusable task definitions.

CREATE TABLE HR_OnboardingTaskTemplate
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    TaskName NVARCHAR(200) NOT NULL,

    ResponsibleDepartment NVARCHAR(100) NOT NULL,

    IsMandatory BIT NOT NULL DEFAULT(1),

    IsActive BIT NOT NULL DEFAULT(1)
);

Examples:

Create Email Account
Allocate Laptop
Issue Access Card
Assign Buddy
Prepare Workstation
Payroll Registration
Table 3: Onboarding Task

Actual task instances.

CREATE TABLE HR_OnboardingTask
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    OnboardingFk NUMERIC(18,0) NOT NULL,

    TaskTemplateFk NUMERIC(18,0) NOT NULL,

    AssignedToEmployeeFk NUMERIC(18,0) NULL,

    DueDate DATE NULL,

    Status NVARCHAR(50) NOT NULL,

    CompletedDate DATETIME2 NULL,

    Remarks NVARCHAR(1000) NULL
);

Status:

Pending
InProgress
Completed
Cancelled
Table 4: Onboarding Document

Documents submitted by candidate.

CREATE TABLE HR_OnboardingDocument
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    OnboardingFk NUMERIC(18,0) NOT NULL,

    DocumentType NVARCHAR(100) NOT NULL,

    FileName NVARCHAR(500) NOT NULL,

    FilePath NVARCHAR(1000) NOT NULL,

    UploadedDate DATETIME2 NOT NULL
);

Examples:

CNIC
Passport
Degree
Experience Letter
Bank Details
Tax Forms
Photo
Table 5: Joining Confirmation

This is the trigger for employee creation.

CREATE TABLE HR_JoiningConfirmation
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    OnboardingId NUMERIC(18,0) NOT NULL,

    JoinedDate DATE NOT NULL,

    ConfirmedByEmployeeFk NUMERIC(18,0) NOT NULL,

    Remarks NVARCHAR(1000) NULL,

    ConfirmedDate DATETIME2 NOT NULL
);
Integration with Existing Employee Module

When Joining Confirmation is completed:

Candidate
    ↓
Create Employee
    ↓
Generate Employee Code
    ↓
Create User Account
    ↓
Assign Roles

Use a service:

IEmployeeCreationService

Example:

CreateEmployeeFromCandidate(
    candidateId,
    onboardingId
);
New Status Flow
Onboarding
Initiated
DocumentsPending
TasksInProgress
ReadyToJoin
Joined
Completed
Cancelled
Automation Events

When onboarding starts:

Send Email To Candidate
Send Email To IT
Send Email To Manager

When joined:

Create Employee
Create User Account
Assign Default Role
Deliverable After Sprint 7

Complete workflow:

Resignation
    ↓
Requisition
    ↓
Approval
    ↓
Recruitment
    ↓
Interview
    ↓
Offer
    ↓
Accepted
    ↓
Onboarding
    ↓
Joined
    ↓
Employee Created
-------------------------------------------------------------
		Sprint 8 - Later
-------------------------------------------------------------
At this point, the recruitment workflow is essentially complete.

If you're building a real enterprise HRMS, the next sprint should not be another recruitment table. It should be the infrastructure that makes all HR modules work consistently.

Sprint 8: Notification Center + Activity Center

Right now every module will eventually need:

Recruitment
Leave
Claims
Travel
Performance
Promotion
Separation

All of them generate:

Approvals
Reminders
Alerts
Tasks
Emails

If you build notifications separately inside each module, maintenance becomes painful.
We already have recruitment and onboarding modules.

Now implement a centralized Notification Center.

Requirements:

Support Email notifications.
Support In-App notifications.
Support future SMS integration.
Support notification templates.
Support user notification preferences.
Support read/unread status.
Must be reusable by all HR modules.
Primary key NUMERIC(18,0) IDENTITY.
EF Core decimal type.

Create:

Tables
Services
Background jobs
MVC screens

Design as a shared platform service.

Table 1: Notification Template
CREATE TABLE SYS_NotificationTemplate
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    TemplateCode NVARCHAR(100) NOT NULL,

    SubjectTemplate NVARCHAR(500) NOT NULL,

    BodyTemplate NVARCHAR(MAX) NOT NULL,

    Channel NVARCHAR(50) NOT NULL,

    IsActive BIT NOT NULL DEFAULT(1)
);

Examples:

REQ_SUBMITTED
REQ_APPROVED
INTERVIEW_SCHEDULED
OFFER_SENT
ONBOARDING_STARTED
Table 2: Notification Queue

Never send emails directly from controllers.

Queue them.

CREATE TABLE SYS_NotificationQueue
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    RecipientUserId NUMERIC(18,0) NOT NULL,

    TemplateCode NVARCHAR(100) NOT NULL,

    Subject NVARCHAR(500) NOT NULL,

    Body NVARCHAR(MAX) NOT NULL,

    Status NVARCHAR(50) NOT NULL,

    RetryCount INT NOT NULL DEFAULT(0),

    CreatedDate DATETIME2 NOT NULL,

    SentDate DATETIME2 NULL
);
Table 3: In-App Notification
CREATE TABLE SYS_UserNotification
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    UserId NUMERIC(18,0) NOT NULL,

    Title NVARCHAR(500) NOT NULL,

    Message NVARCHAR(MAX) NOT NULL,

    IsRead BIT NOT NULL DEFAULT(0),

    ReadDate DATETIME2 NULL,

    CreatedDate DATETIME2 NOT NULL
);
Table 4: User Preferences
CREATE TABLE SYS_UserNotificationPreference
(
    Id NUMERIC(18,0) IDENTITY(1,1) PRIMARY KEY,

    UserId NUMERIC(18,0) NOT NULL,

    NotificationType NVARCHAR(100) NOT NULL,

    EmailEnabled BIT NOT NULL DEFAULT(1),

    InAppEnabled BIT NOT NULL DEFAULT(1)
);
Background Service

Create:

INotificationService
IEmailSender
INotificationTemplateService

Background worker:

NotificationBackgroundService

Runs every minute.

Process:

Pending
 ↓
Sending
 ↓
Sent
Recruitment Integration

Examples:

Requisition Submitted
Notify HRBP
Requisition Approved
Notify Recruiter
Interview Scheduled
Notify Candidate
Notify Interviewers
Offer Sent
Notify Candidate
Employee Joined
Notify HR
Notify Manager
Notify IT
Sprint 9 (After Notifications)

The next thing I would build is something every enterprise HR system eventually needs:

Activity Feed
Audit Logs
Timeline

Instead of users asking:

What happened to Requisition #123?

They can open a timeline and see:

01-Jun Created
02-Jun Submitted
03-Jun HRBP Approved
04-Jun HOD Approved
05-Jun Recruiter Assigned
06-Jun Job Posted
10-Jun Candidate Applied
15-Jun Interview Scheduled
25-Jun Offer Sent
30-Jun Joined

That single feature dramatically improves usability and supportability across the entire system.

If I were prioritizing after Sprint 7, I'd do:

Sprint 8 → Notification Center
Sprint 9 → Audit & Timeline
Sprint 10 → Recruitment Analytics Dashboard
Sprint 11 → Background Verification
Sprint 12 → Recruitment Agencies & Vendor Portal

Those are the features that move the product from a working HRMS to an enterprise HR platform.